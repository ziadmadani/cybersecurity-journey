Objective: Find the password stored in the home directory and use it to log into bandit1.

connecting via SSH the command line is 'ssh bandit0@bandit.labs.overthewire.org -p 2220'

listing the existing files with ls which list the file 'readme'

opening the file 'readme' with cat that gave us the required password
